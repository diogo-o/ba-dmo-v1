# Build output
bin/
obj/
*.user

# Local .NET SDK installed for the fresh build workstation (not part of the application)
.dotnet-sdk/
dotnet-install.ps1

# IDE
.vs/
.idea/
*.suo

# Test results
TestResults/
coverage/

# Qoder IDE workspace metadata
.qoder/

# Local secret-bearing dev artifacts (SEC-01 / CR-1 coordinated hygiene)
.env
.env.*
dbdiag/
.tmp-*
session-cookies.txt
start-debug.ps1

# Agent tooling workspace state (never commit; bundles node_modules / worktrees)
.opencode/
.kilo/

# Compiled smoke/publish build output (rebuilt from src/; not source)
freeze-smoke-build/

# Transient logs, build records and runtime HTML snapshots
*.log
*.log.err
*.binlog
msbuild.log
full_build.log
build_output.log
build_verify.log
build.txt
build-x.txt
.infra-restore.txt
.restore-log.txt
app-run.log
freeze-smoke-app.log
dmo-5051.log
# one-off DB batch/checkpoint dumps (durable .md audit records stay tracked)
reports/batch_B0_result.txt
reports/batch_B1_result.txt
reports/db_checkpoint_B1.txt
reports/remediation_git_state.txt
live-admin.html
live-admin-users.html
live-admin-users-5051.html
live-jobon.html
